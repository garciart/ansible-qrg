---
# Ansible inventory using YAML format
# ansible-inventory -i inventory2.yml --graph
# ansible-inventory -i inventory2.yml --list -y
all:
  hosts:
    control_node:
      # Associate key-value pair with host
      ansible_connection: local
    managed_node_1:
    managed_node_2:
    orphan_node:
      ansible_connection: local

  children:
    control_nodes:
      hosts:
        control_node:

    managed_nodes:
      vars:
        # Associate key-value pair with group
        ansible_connection: local
      hosts:
        managed_node_1:
        managed_node_2:
...
# code: language=yaml
# vi: set noai nu ts=2 sw=2 sts=2 sta et:
