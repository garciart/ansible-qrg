# code: language=yaml
# vi: tabstop=2 shiftwidth=2 softtabstop=2 expandtab:
---
all:
  hosts:
    local_node:
      ansible_connection: local
  children:
    control_node:
      hosts:
        local_node:
    managed_nodes:
      hosts:
        local_node:
